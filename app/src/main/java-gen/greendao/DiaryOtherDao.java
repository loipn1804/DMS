package greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import greendao.DiaryOther;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table DIARY_OTHER.
*/
public class DiaryOtherDao extends AbstractDao<DiaryOther, Long> {

    public static final String TABLENAME = "DIARY_OTHER";

    /**
     * Properties of entity DiaryOther.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property Diary_type_id = new Property(1, Long.class, "diary_type_id", false, "DIARY_TYPE_ID");
        public final static Property Content = new Property(2, String.class, "content", false, "CONTENT");
        public final static Property Created_by = new Property(3, Long.class, "created_by", false, "CREATED_BY");
        public final static Property Created_at = new Property(4, String.class, "created_at", false, "CREATED_AT");
        public final static Property Status = new Property(5, Long.class, "status", false, "STATUS");
        public final static Property Created_by_name = new Property(6, String.class, "created_by_name", false, "CREATED_BY_NAME");
    };


    public DiaryOtherDao(DaoConfig config) {
        super(config);
    }
    
    public DiaryOtherDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'DIARY_OTHER' (" + //
                "'ID' INTEGER PRIMARY KEY ," + // 0: id
                "'DIARY_TYPE_ID' INTEGER," + // 1: diary_type_id
                "'CONTENT' TEXT," + // 2: content
                "'CREATED_BY' INTEGER," + // 3: created_by
                "'CREATED_AT' TEXT," + // 4: created_at
                "'STATUS' INTEGER," + // 5: status
                "'CREATED_BY_NAME' TEXT);"); // 6: created_by_name
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'DIARY_OTHER'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, DiaryOther entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long diary_type_id = entity.getDiary_type_id();
        if (diary_type_id != null) {
            stmt.bindLong(2, diary_type_id);
        }
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(3, content);
        }
 
        Long created_by = entity.getCreated_by();
        if (created_by != null) {
            stmt.bindLong(4, created_by);
        }
 
        String created_at = entity.getCreated_at();
        if (created_at != null) {
            stmt.bindString(5, created_at);
        }
 
        Long status = entity.getStatus();
        if (status != null) {
            stmt.bindLong(6, status);
        }
 
        String created_by_name = entity.getCreated_by_name();
        if (created_by_name != null) {
            stmt.bindString(7, created_by_name);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public DiaryOther readEntity(Cursor cursor, int offset) {
        DiaryOther entity = new DiaryOther( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // diary_type_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // content
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // created_by
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // created_at
            cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5), // status
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // created_by_name
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, DiaryOther entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDiary_type_id(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setContent(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCreated_by(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setCreated_at(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStatus(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setCreated_by_name(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(DiaryOther entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(DiaryOther entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
